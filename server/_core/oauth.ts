/**
 * This file previously contained the Manus OAuth callback route.
 * It has been replaced by standard email/password authentication.
 *
 * Auth routes are now registered in `authRoutes.ts`:
 *   POST /api/auth/register
 *   POST /api/auth/login
 *   POST /api/auth/logout
 */

export {};
